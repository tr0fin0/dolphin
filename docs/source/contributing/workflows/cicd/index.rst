`cicd <.github/workflows/cicd.yml>`_
====================================

This workflow verifies whenever a pull request is altered if it's name respects the :ref:`commit-messages` structure and adds labels based on the it's contents and name.
